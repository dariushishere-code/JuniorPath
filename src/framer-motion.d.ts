// Ambient declarations for framer-motion.
// NOTE: The framer-motion package installed in node_modules was shipped without
// its .d.ts files (stripped by the registry mirror). These declarations keep
// TypeScript happy while preserving runtime behavior. If you later restore the
// real types, remove this file and run `npm install`.
declare module 'framer-motion';